services:
  - type: web
    name: emo-emotional-companion
    runtime: node
    rootDir: emo-pet-backend
    buildCommand: npm install
    startCommand: node server.js
    envVars:
      - key: NODE_ENV
        value: production
